# Gemini API Configuration
# Get your API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Gmail API Configuration
# Option 1: Path to service account JSON file
GOOGLE_SERVICE_ACCOUNT_FILE=/app/credentials/service-account.json

# Option 2: Service account JSON as string (alternative to file)
# GOOGLE_CREDENTIALS_JSON={"type": "service_account", ...}

# Optional: For domain-wide delegation with service accounts
# GMAIL_DELEGATED_USER=user@yourdomain.com

# Optional: Path to credentials directory (for docker-compose)
CREDENTIALS_PATH=./credentials

# Application Port
PORT=5000
